services:
  wealthwatcher-spec:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./SPEC.md:/app/SPEC.md:ro
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/data/wealthwatcher.db
